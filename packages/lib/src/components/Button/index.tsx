import React from "react"
import styled from "styled-components"

const StyledButton = styled.button`
  background: red;
`

export const Button = () => <StyledButton>Hello this is button</StyledButton>
